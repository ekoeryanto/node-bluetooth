{
  "name": "node-bluetooth",
  "version": "1.2.3",
  "description": "Bluetooth serial port communication for Node.js",
  "keywords": [
    "bluetooth"
  ],
  "main": "index.js",
  "os": [
    "darwin",
    "linux",
    "win32"
  ],
  "dependencies": {
    "nan": "~2.13.0",
    "bindings": "1.2.x",
    "prebuild-install": "^5.3.2"
  },
  "engines": {
    "node": ">=0.8.x <12.x",
    "npm": ">=5.2.x"
  },
  "scripts": {
    "install": "prebuild-install || node-gyp configure build",
    "install-debug": "prebuild-install --debug || node-gyp configure build --debug",
    "prebuild": "prebuild --all --force --strip --verbose"
  },
  "license": "MIT",
  "author": "lsong",
  "directories": {
    "example": "example"
  },
  "devDependencies": {
    "prebuild": "^9.1.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ekoeryanto/node-bluetooth.git"
  },
  "bugs": {
    "url": "https://github.com/ekoeryanto/node-bluetooth/issues"
  },
  "homepage": "https://github.com/ekoeryanto/node-bluetooth#readme"
}
